# prompt-engineer — examples

Input → prompt shape (abbrev).

| Input | Prompt focus |
|-------|----------------|
| `make api faster` | perf engineer; bottlenecks + low-risk wins; profile evidence; top 3 + validate |
| `review PR auth JWT sessions` | polish only: scope in/out, severity tags, ship/block summary |
| `terraform vs pulumi` | criteria table, matrix, rec, migration risks |
| `explain like i'm new` | newcomer audience; what/why, concepts, glue, glossary; user attaches doc |

**Polish-only:** keep user wording; add format + scope + done-when.
